class Foo { Str foo(Obj v) {
          line := "foo $v."
          return line } }

class Foo2 { Str foo(Obj v) {
          line := "foo $v. "
          return line } }

class Foo3 { Str foo(Obj v) {
          line := "foo $v.123"
          return line } }